import Link from "next/link";
import { Progress } from "../../components/ui/progress";
export function DeathConfirming({ approvedCount, requiredCount }: Readonly<{ approvedCount: number; requiredCount: number }>) { return <section className="dls-public-stage dls-enter"><div className="dls-ring"><strong>{approvedCount} / {requiredCount}</strong><span>确认进度</span></div><h1>生存状态确认中</h1><p>系统已进入第一阶段确认。达到预设门限后才会进入最终等待期；此页面只公开聚合进度。</p><Progress label="生存状态确认进度" max={requiredCount} value={approvedCount} /><div className="dls-public-actions"><Link className="dls-button dls-button--primary" href="/contact/login">紧急联系人登录</Link><Link className="dls-button dls-button--secondary" href="/legal">查看操作说明</Link></div><div className="dls-public-info"><section><h2>身份完全隐藏</h2><p>不会显示任何联系人的身份、姓名、邮箱、决定或时间。</p></section><section><h2>70% 门限逻辑</h2><p>达到服务端冻结的确认门限后，系统才进入最终发布等待期。</p></section></div></section>; }
